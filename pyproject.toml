[project]
name = "andy-a11y-analyzer"
version = "0.1.0"
description = "A11y Analyzer Backend"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests",
    "beautifulsoup4",
    "lxml"
]

[tool.uv]
managed = true
